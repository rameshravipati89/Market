apiVersion: apps/v1
kind: Deployment
metadata:
  name: localadmin-frontend
  namespace: mlm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: localadmin-frontend
  template:
    metadata:
      labels:
        app: localadmin-frontend
    spec:
      containers:
        - name: localadmin-frontend
          image: ghcr.io/rameshravipati89/market-localadmin-frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: localadmin-frontend
  namespace: mlm
spec:
  type: NodePort
  selector:
    app: localadmin-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000      # accessible at http://<node-ip>:30000
